# 🔧 知识图谱体检报告 + Bug 修复清单（2026-08-05）

> **创建**: 2026-08-05 · **状态**: 📋 待修复 · **作者**: 铃铛（体检报告分析项目）
> **关联**: [[1-Projects/体检报告分析]] · [[医学实体关系抽取]]

---

## 🎯 背景

老板（开发者）在 8/5 讨论了知识图谱建模问题：规则应保留为节点还是抽成关系、贫血推理链路。检查了 Neo4j 图谱（bolt://127.0.0.1:7687，neo4j/neo4j）后，发现一个**必须修的 bug** 和多个待补项，并最终敲定了**目标架构**（见文末第四节）。

**图谱规模**：642 节点 / 503 关系 / 平均度 1.57

---

## 🐛 P0 Bug：evidence_json 转义错误（明天优先修）

### 问题

多个节点的 `evidence_json` 里 quote 含 LaTeX 转义符（`\(`、`\[`、`\)`），导致 **`json.loads` 解析失败**：

```
错误: Invalid \escape: line 1 column 74 (char 73)
原始: "exact_quote":"②造血原料缺乏所致的贫血：如铁缺乏引起的缺铁性贫血或叶酸、维生素 \( B_{12} \) 缺乏引起的巨幼细胞贫血。"
```

### 受影响节点（已验证 7 个，可能更多）

- 缺铁性贫血
- 小细胞低色素性贫血
- 正细胞性贫血
- 再生障碍性贫血
- 巨幼细胞贫血
- 急性失血性贫血
- 大红细胞性贫血（推测）

### 根因方向

抽取管线的 quote 清洗/JSON 序列化步骤：`\\(` 或 `\(` 未被转义成 `\\(` 就写入 JSON。**在抽取代码里搜 `exact_quote` 的构造/清洗处**，找到把 LaTeX 原文直接塞进 JSON 的地方。

### 验收标准

对受影响节点执行 `json.loads(evidence_json)` 全部成功，且 quote 完整保留 LaTeX 原文。

---

## 📊 图谱分析结论（供建模参考）

### 1. 孤立节点严重（69%）

- **251/365 MedicalConcept 无任何关系**（100% 来自 `entity-list-v0.4` 管线）
- 原因：实体识别高召回，但关系建立是"语境触发"（只在规则/论断/参考范围语境建关系），识别出的概念未被后续关系管线接住 → 无回填机制
- **不是设计问题，是"识别全量、关系稀疏"的管线断层**

### 2. 贫血推理链断裂（核心业务问题）

体检报告场景链路应为：

```
体检单 HGB 105 + 女性
  ↓ 规则引擎（代码层）✅ 已有
贫血状态 = 已确认（规则：贫血状态 = 血红蛋白贫血状态确认）
  ↓ 图谱查询：形态分型（MCV/MCH/MCHC）✅ 已有
小细胞低色素性贫血
  ↓ 图谱查询：铁代谢模式 → 病因 ❌ 缺失！
缺铁性贫血 / 地中海贫血...
```

**16 个贫血子类中 14 个只有 IS_A → 贫血 一条边**，往下没有"→ 病因/检查建议"的推理边。

**但书里已有依据**（evidence 3 页表格）：
```
血清铁↓ + TIBC↑ → 缺铁性贫血 / 慢性失血 / 需铁量增加
血清铁↓ + TIBC↓ → 慢性感染 / 肝硬变 / 尿毒症...
血清铁↑ + TIBC↓ → 溶血性贫血 / 再障 / 巨幼 / 地中海贫血
```

### 3. 建模决策（已讨论确认）

| 元素 | 结论 |
|---|---|
| Rule 节点 | ✅ 保留为节点（多入多出、规则链依赖、可执行+可追溯） |
| Claim | ✅ 保留（异常→疾病的推理跳板） |
| 贫血状态 | ✅ 保留独立节点（RuleOutputEndpoint / semantic_level=confirmation），不合并进"贫血"疾病节点 |
| 概念语义直达边 | 🆕 建议补：`LOW_INDICATES`/`HIGH_INDICATES`（TestItem→状态概念，1 跳直达） |
| 规则引擎 vs 图谱 | 代码层执行数值判定（PLT<100 等），图谱层做关联推理——**规则不塞 AST 进图** |

---

## 📋 待办清单（明天按序）

- [ ] **P0** 修 evidence_json 转义 bug（含 LaTeX 的 quote）
- [ ] **P1** 补"贫血分型 → 病因"推理边（用血清铁-TIBC 判别表，书 3/16 页）
- [ ] **P1** 孤立概念回填：250 个孤立概念中挑贫血相关病因（钩虫病→缺铁、肝破裂→失血）
- [ ] **P2** 建模贫血状态：保留独立节点，规则引擎负责计算
- [ ] **P2** 后续章节推进时：图谱专注存知识+证据，规则全在引擎层

---

## 🏗️ 第四节：目标架构（2026-08-05 晚，老板确认）

> 经过多轮讨论，最终敲定「**规则引擎 + 状态中心图谱 + LLM 推理**」三层架构。

### 4.1 架构总览

```
体检单（HGB 105, 女性, PLT 80...）
    │
    ▼ ① 规则引擎先跑（代码层，不碰图谱）
输出：命中的状态集合 { HGB偏低, PLT减少, 贫血状态已确认 }
    │
    ▼ ② 拿状态查图谱（图谱不存任何规则逻辑）
(血红蛋白)-[HAS_STATE]->(血红蛋白偏低)-[SEEN_IN]->(缺铁性贫血)
(血小板计数)-[HAS_STATE]->(血小板减少)-[SEEN_IN]->(再生障碍性贫血)
    │
    ▼ ③ LLM 生成建议（报告原文 + 状态 + 图谱路径）
```

### 4.2 图谱存什么 / 不存什么

| 元素 | 存/走 | 说明 |
|---|---|---|
| 检验项 TestItem | ✅ 存 | 体检报告入口锚点 |
| **状态概念**（RuleOutputEndpoint） | ✅ **核心** | 血红蛋白偏低 / 血小板减少 / 贫血状态——规则跑完的**可能结果** |
| 疾病 / 病因 | ✅ 存 | 缺铁性贫血 / 再生障碍性贫血 |
| 参考范围 ReferenceRange | ✅ 存（优化） | 知识展示 + 审计；**判定能力由引擎消费其边界** |
| 人群 Population | ✅ 存 | 性别 / 成人 |
| 语义关系（Claim 拍平） | ✅ 存 | SEEN_IN / LEADS_TO / DEFINES_AS / MONITORING_GUIDANCE / DIFFERENTIAL_DIAGNOSIS |
| **复杂规则逻辑**（AST/cases） | ❌ **不存** | 全在引擎层 |
| **Rule 节点** | ⚠️ 退役 | 21 个规则节点的可执行逻辑清掉，只留声明骨架或整体退役 |

### 4.3 关键建模决策（已确认）

1. **Claim 从节点 → 关系**：43 个 Claim 节点拍平成语义关系，`evidence_json` 挂关系属性
   - 语义类型 = 关系类型：SEEN_IN / LEADS_TO / DEFINES_AS / MONITORING_GUIDANCE / DIFFERENTIAL_DIAGNOSIS
   - 查询从 3 跳变 1 跳：`MATCH (t)-[:SEEN_IN]->(d) WHERE t.name CONTAINS '血小板'`
2. **参考范围 = 简单规则源**：保留为知识节点（min/max/unit/sex/method/evidence），引擎消费其边界 → 输出偏高/偏低状态
3. **状态概念 ≠ 本次体检状态**：图谱存静态"血红蛋白偏低"概念；引擎/会话存动态"这次 HGB=105 偏低"（用完即弃，不入图）
4. **节点 vs 关系判断准则**：
   > 有「多入多出 / 依赖链 / 被再次指向」→ 节点（如复杂 Rule）
   > 只是「A 语义关联 B」→ 关系（如 Claim）

### 4.4 LLM 自查发现（图法官审查 43 条 Claim）

| 问题 | 数量 |
|---|---|
| 复合结论 Claim（需拆 6 条） | 1 条 |
| 结论非疾病（语义越界：外科手术/化疗以后/用药） | 3 条 |
| 残缺/自指 Claim | 3 条 |
| APPLIES_TO 语义错乱（指向"性别"抽象属性） | 2 条 |
| **合计（占 21%）** | **~9 条** |

### 4.5 本体层（EDC：Extract → Define → Canonicalize）

- **Extract**（7 类实体）：MedicalConcept(Disease/Etiology/MethodOrDrug/RuleOutputEndpoint) / TestItem / ReferenceRange / Population / TestMethod / Claim / SourceLocator
- **Define**（关系语义域约束）：
  - SEEN_IN / CLAIM_HAS_CONCLUSION 目标必须是 **Disease**（非疾病 → 归入 Situation/Exposure 新类型）
  - APPLIES_TO 源只能是 ReferenceRange/规则 → 具体人群，禁止指向"性别"抽象属性
  - HAS_REFERENCE_RANGE: TestItem → ReferenceRange
- **Canonicalize**（规范化规则）：
  - ALIAS_OF 统一方向：别名 → 标准名
  - Claim 一条一结论：复合结论必须拆分
  - Rule 命名：name 只留"贫血程度分级"，签名拆到 properties
  - ReferenceRange 名称去内部 ID：`血红蛋白参考范围/chapter01:reference:.../1` → `血红蛋白参考范围-男性`

### 4.6 参考范围建模优化

- 名称去掉内部 ID
- 区分维度显式化成属性：sex / method / min / max / unit
- 同一检验项的多个参考范围用兄弟边串起：`(血红蛋白)-[:HAS_REFERENCE_RANGE]->(男/女两套)`

### 4.7 条件信息分工（性别/年龄放哪，已确认）

**核心原则：图谱存"适用于谁"（声明），引擎存"怎么判定"（逻辑），同一条件不双写。**

现状问题：同一性别条件被重复三份（`condition_ast` + `applicability.required_inputs` + `APPLIES_TO`）——改一处漏两处。

| 位置 | 存什么 | 例子 |
|---|---|---|
| **规则引擎（代码层）** | 判定逻辑（唯一执行源） | `if 性别=='女' and HGB<115: return '已确认'` |
| **图谱** | 适用人群声明 + 证据 | `(规则)-[APPLIES_TO]->(女性)`，无执行逻辑 |

- 需要年龄/性别的规则（贫血状态确认、贫血程度分级）：**条件只写进引擎**，图谱只留 APPLIES_TO 声明
- `condition_ast` 从图谱退役；`required_inputs` 可保留为声明但不承载逻辑
- 这样规则随便加，图谱零改动；图谱能回答"这规则适用于谁"，引擎负责执行

### 4.8 人群建模（避免"男性/女性关联爆炸"，已确认）

**核心原则：常规切分维度用属性，特殊人群才用 APPLIES_TO 关系。**

现状：男性被引用 12 次、女性 16 次；若全部 APPLIES_TO → 各被连 20+ 次，星型结构无信息量。

| 人群类型 | 建模 | 例子 |
|---|---|---|
| **性别/年龄**（常规切分维度） | **属性** `sex/age_group` | `(参考范围){sex:'female', min:115, max:150}` |
| **生理状态**（特殊人群） | **关系** APPLIES_TO | 妊娠期 / 哺乳期 / 老年人 |
| **Patient → 人群** | 关系 BELONGS_TO | 张三 -[BELONGS_TO]-> 女性 |

- **判断标准**：这个人群是"常规切分维度"（所有检验项都可能按它分）→ 属性；是"特殊适用对象"（只有部分规则/参考范围专门为它设计）→ 关系
- 后续章节（尿常规/生化）都按此规矩：sex/age 做成 ReferenceRange 和 Rule 的公共属性，避免人群节点被几百条边淹没
- 修正现有 2 条错误边：`(规则)-[APPLIES_TO]->(性别)` → 删除或改为指向具体人群

### 4.9 Patient（人物）层设计（已确认）

- 新增 `Patient` 标签：体检者节点，属性 `gender` / `age`（性别/年龄当属性存，不建节点）
- `Patient -[BELONGS_TO]-> Population`：连到图谱已有的人群分类（女性/成人）
- `Patient -[HAS_STATE]-> 状态实例`：挂当次推断结果（贫血状态=已确认，时间戳），用完即弃不入静态图谱
- 性别/年龄三重角色：**规则引擎的判定输入** + **图谱查询的过滤条件**（WHERE）+ **BELONGS_TO 的语义连接**

### 4.10 最终实施顺序（明天）

1. 🐛 **P0** 修 evidence_json 转义 bug（含 LaTeX 的 quote）
2. 🏗️ **架构调整**：Claim 拍平为关系 + Rule 逻辑退役 + 参考范围属性化 + condition_ast 移除
3. 🔧 **自查修正**：9 条 Claim 拆分/删越界/补残缺 + APPLIES_TO 修正（指向"性别"的 2 条删除/改向）
4. 🔗 **贫血推理链补齐**：血清铁-TIBC 判别表建 SUGGESTS 边 + 孤立概念回填
5. 📦 **实体规范化**：ALIAS_OF 批量补齐（巨幼红细胞/巨幼细胞、地中海/珠蛋白）
6. 👤 **Patient 层**：新增 Patient 标签 + BELONGS_TO → Population
7. ✅ **验收断言**：用 4.5 的 Define 约束当验收标准（如 SEEN_IN 目标必须 Disease）

---

## 🔗 相关链接

- [[1-Projects/体检报告分析/README]]
- [[1-Projects/体检报告分析/任务系统/医学实体关系抽取]]
- [[1-Projects/体检报告分析/任务系统/Demo 流水线 v0.1]]
- 图谱：bolt://127.0.0.1:7687（neo4j/neo4j）

---

*创建: 2026-08-05 · 维护: 铃铛*
